

const SectionHeader = ({ section_title }) => {
    return (
        <h1 className='text-2xl sm:text-3xl md:text-3xl font-bold my-5 uppercase'>{section_title}</h1>
    );
};

export default SectionHeader;